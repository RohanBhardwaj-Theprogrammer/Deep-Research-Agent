# Copy to .env and customize
MONGODB_URI=mongodb://localhost:27017/deepresearch

# LM Studio runs a local server with OpenAI-compatible API
# Default URL when LM Studio server is started locally:
LMSTUDIO_BASE_URL=http://localhost:1234/v1
LMSTUDIO_MODEL=lmstudio-community/Meta-Llama-3.1-8B-Instruct-GGUF

# Server
PORT=4567
